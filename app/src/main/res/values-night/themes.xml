<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Group3_Final_Project" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- App KHÔNG có bản thiết kế dark mode riêng (toàn bộ layout dùng nền trắng/chữ đen
             cứng) — giữ nguyên đúng màu thương hiệu ở đây để tránh rơi về tím mặc định của
             Material3 khi máy người dùng bật Dark Mode hệ thống. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/color_accent</item>
        <item name="android:windowBackground">@color/color_background</item>
        <item name="colorError">@color/color_error</item>
    </style>
</resources>