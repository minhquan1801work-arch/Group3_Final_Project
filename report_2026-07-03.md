# 📋 GLASSITY — BÁO CÁO TIẾN ĐỘ

> **Cập nhật:** 03/07/2026 | **Deadline:** ~09/07 | **Branch đang làm:** `feature/person-MinhQuan`
> Minh Quân (Person A) — `ui/account` + `ui/order` | Hồng Phúc (Person B) — `ui/catalog`

---

## 🔧 Tech stack (chốt, không đổi)

Java thuần (không Kotlin) · Repository + Callback (không MVVM) · ViewBinding · Firebase Auth + Cloud Firestore · Cloudinary cho ảnh (cloud: aa1g9udv) · Glide · BottomNavigationView + NavHostFragment

---

## ✅ ĐÃ HOÀN THÀNH (tính đến 03/07)

### Nền tảng
- Firebase project `glassity-770c5`, google-services.json chuẩn, SHA-1 đã add
- 20 sản phẩm + 4 categories đã seed, ảnh Cloudinary
- Toàn bộ Models + Repositories (Product/Cart/Order/Category/Auth) — **chỉ gọi, không viết thêm**
- **5 account test** (password chung `Test@123`) — seed bằng `node seed_test_accounts.js`

| Email | Data có sẵn để test |
|---|---|
| test1@glassity.com | 3 đơn (DELIVERED / SHIPPED / PENDING-COD), giỏ 2 items, favorites, notifications |
| test2@glassity.com | 1 đơn DELIVERED đã thanh toán bank, giỏ 3 items |
| test3@glassity.com | Đơn CANCELLED + đơn PROCESSING, giỏ trống |
| test4@glassity.com | Giỏ 1 item + 4 favorites, chưa mua |
| test5@glassity.com | Trống hoàn toàn — test empty states |

### Màn hình đã code xong
| Màn hình | Người làm | Ghi chú |
|---|---|---|
| Welcome / Login / Signup / ForgotPassword | ✅ | Google Sign-In: config đúng, lỗi trên emulator là do chưa add Google account vào emulator |
| HomeFragment | ✅ | Full layout Figma |
| CategoryFragment + ProductListActivity | ✅ | Filter chip mới có UI |
| **CartFragment (DL_Cart)** | ✅ A1 | 3 trạng thái (chưa login / trống / có hàng), checkbox từng item + Tất cả, stepper ± ghi Firestore, tổng theo item tick, MUA HÀNG (n) |
| **CheckoutActivity (LA.Payment)** | ✅ A2 | Địa chỉ (sửa + lưu Firestore), ship tiêu chuẩn 35k / nhanh 50k, voucher (nhập tay + BottomSheet: FREESHIP / GIAM10 / GIAM50K), COD / chuyển khoản, chi tiết thanh toán đủ dòng, ĐẶT HÀNG → tạo order + orderDetails + payment, chỉ xóa item đã mua khỏi giỏ |
| **PaymentResultActivity (LA_Pay)** | ✅ | COD → "ĐẶT HÀNG THÀNH CÔNG" / Bank → "ĐANG CHỜ THANH TOÁN" + info chuyển khoản (nội dung CK = mã đơn), grid gợi ý sản phẩm |
| **Footer floating pill** | ✅ | Dark pill bo tròn, chỉ icon, badge đỏ số giỏ hàng realtime, hide-on-scroll (cuộn xuống ẩn, kéo ngược hiện — kiểu Facebook) |

### UI đã restyle theo Figma (03/07)
- Nền **TRẮNG toàn app** (`color_background` → white, bỏ nền kem)
- Nút **ĐEN** (`bg_btn_black_filled/outline`), giá **CAM** `#EE4D2D` (`color_price`)
- Item giỏ: chip "Màu: đen ▾", kẻ phân cách mảnh, không icon thùng rác (xóa = bấm − khi số lượng 1)
- Footer: `activity_main.xml` (pill) + `bottom_nav_menu.xml` (icon) + `nav_item_color_dark.xml` (màu icon) + badge trong `MainActivity.setupCartBadge()`

### Bug đã sửa (quan trọng — đọc kỹ)
1. **`@DocumentId` crash**: KHÔNG BAO GIỜ ghi field id (orderId, cartDetailId, customerId...) vào document Firestore — model dùng `@DocumentId`, có field trùng tên là `toObjects()` crash app. Seed script đã sửa. `CartRepository.getCartItems()` + `CheckoutActivity.loadCustomer()` đã chống crash bằng map thủ công.
2. Google Sign-In "communicating with Google servers" → fix trên emulator: Settings → Accounts → add Google account. Code không cần sửa.

---

## 🅱️ CHECKLIST CHO HỒNG PHÚC (Person B — branch `feature/person-b`)

Làm theo thứ tự, chỉ đụng file trong `ui/catalog` + `adapter`:

- [x] **[B1] ProductDetailActivity** — ✅ XONG 03/07 (làm trên branch `feature/person-MinhQuan`)
  - `ui/catalog/ProductDetailActivity.java` + `activity_product_detail.xml` + `item_review.xml` + `item_image_thumb.xml`
  - ViewPager2 ảnh + thumbnail viền cam, giá cam, tabs Đánh giá/Chi tiết/Dành cho bạn (cuộn anchor), chọn màu (dot động từ `colors`, map hex → tên tiếng Việt), stepper ±, bảng kích thước, review demo (TODO: collection `reviews`), Sản phẩm liên quan (cùng category)
  - "Thêm vào giỏ" → `addToCart()`; "MUA NGAY" → `addToCartReturningId()` (method MỚI trong CartRepository) → `CheckoutActivity.start(item vừa thêm)` — đúng BPMN, checkout xong tự xóa item khỏi giỏ
  - Guest bấm mua → đưa về LoginActivity
- [x] **[B2] Nối ProductDetail vào app** — ✅ XONG 03/07: HomeFragment (cả featured + grid), ProductListActivity, PaymentResultActivity.loadSuggestions; Manifest đã thêm activity
- [ ] **[B-mới] Mô tả sản phẩm ~40 chữ**: chạy `npm install firebase-admin && node update_descriptions.js` (cần service-account.json) — script match theo `name`, chỉ update field `description`
- [ ] **[B3] btnFilter trong ProductListActivity** — BottomSheet lọc giá/loại
- [ ] **[B4] HomeFragment** — 5 circle "chọn kính theo dạng mắt" → ProductListActivity có filter
- [ ] Style mới cần theo: nền trắng, nút đen `bg_btn_black_filled`, giá cam `color_price` — xem `fragment_cart.xml` làm mẫu
- [ ] Manifest: chỉ THÊM `<activity>` mới, không xóa/sửa cái có sẵn

**Quy tắc git:** mỗi người 1 branch, xong task lớn → merge `main` → người kia pull. KHÔNG cùng sửa 1 file.

---

## 🅰️ VIỆC TIẾP THEO CỦA MINH QUÂN (Person A)

- [ ] **Test luồng A→Z bằng account MỚI**: Đăng ký mới → (chờ B1 xong: chọn sản phẩm → thêm giỏ) → giỏ hàng → checkout → đặt COD + đặt Bank → kiểm tra Firestore (orders/payments/carts)
  - Tạm thời chưa có ProductDetail: test bằng test1/test2 (giỏ có sẵn)
- [ ] **Front-end khớp Figma từng màn** — so screenshot app vs frame Figma, chỉnh chi tiết (đang chờ cap màn hình)
- [ ] **[A3] ProfileFragment full** (Figma LA_Profile): avatar, tên, email, SĐT, menu Đơn hàng của tôi / Địa chỉ / Đổi mật khẩu / Đăng xuất
- [ ] **[A4] OrderHistoryActivity** (LA_Orders) + OrderDetailActivity — nhớ tạo composite index `orders(customerId ASC, createdAt DESC)` (lần đầu chạy sẽ có link trong Logcat)
- [ ] Nút "XEM ĐƠN HÀNG" trong PaymentResult đang Toast → nối vào A4

---

## ⚠️ LƯU Ý KHI CLONE / CHẠY

1. `service-account.json` KHÔNG có trên git (đã gitignore — file private key). Cần seed lại data thì xin file từ Minh Quân, đặt vào root, chạy `npm install firebase-admin && node seed_test_accounts.js`
2. Sau khi pull code: **File → Reload All from Disk → Sync Gradle → Rebuild**
3. Đăng nhập test: `test1@glassity.com` / `Test@123`
