# 📋 GLASSITY — BÁO CÁO TIẾN ĐỘ

> **Cập nhật:** 04/07/2026 | **Deadline:** ~09/07 | **Branch đang làm:** `feature/person-MinhQuan`
> Minh Quân (Person A) — `ui/account` + `ui/order` | Hồng Phúc (Person B) — `ui/catalog`

---

## 🔧 Tech stack (chốt, không đổi)

Java thuần (không Kotlin) · Repository + Callback (không MVVM) · ViewBinding · Firebase Auth + Cloud Firestore · Cloudinary cho ảnh (cloud: aa1g9udv) · Glide · **Footer pill tự code** + NavHostFragment

---

## ✅ ĐÃ HOÀN THÀNH (tính đến 04/07)

### Nền tảng
- Firebase project `glassity-770c5`, google-services.json chuẩn, SHA-1 đã add
- 20 sản phẩm + 4 categories đã seed, ảnh Cloudinary
- Toàn bộ Models + Repositories (Product/Cart/Order/Category/Auth/**Address**) — **chỉ gọi, không viết thêm**
- **5 account test** (password chung `Test@123`) — seed bằng `node seed_test_accounts.js`

| Email | Data có sẵn để test |
|---|---|
| test1@glassity.com | 3 đơn (DELIVERED / SHIPPED / PENDING-COD), giỏ 2 items, favorites, notifications |
| test2@glassity.com | 1 đơn DELIVERED đã thanh toán bank, giỏ 3 items |
| test3@glassity.com | Đơn CANCELLED + đơn PROCESSING, giỏ trống |
| test4@glassity.com | Giỏ 1 item + 4 favorites, chưa mua |
| test5@glassity.com | Trống hoàn toàn — test empty states |

### Màn hình đã code xong
| Màn hình | Người làm | Ghi chú |
|---|---|---|
| Welcome / Login / Signup / ForgotPassword | ✅ | Lỗi hiện TIẾNG VIỆT; Google Sign-In test OK trên emulator (xem mục Auth bên dưới) |
| HomeFragment | ✅ | Full layout Figma |
| CategoryFragment + ProductListActivity | ✅ | Filter chip mới có UI |
| ProductDetailActivity (B1+B2) | ✅ B | ViewPager2 ảnh, tabs, chọn màu, MUA NGAY → Checkout |
| CartFragment (DL_Cart) | ✅ A | 3 trạng thái, checkbox, stepper, MUA HÀNG (n) |
| CheckoutActivity (LA.Payment) | ✅ A | Ship 35k/50k, voucher, COD/Bank, ĐẶT HÀNG → order + payment |
| PaymentResultActivity (LA_Pay) | ✅ | COD thành công / Bank chờ thanh toán + info CK |
| **Footer pill (component)** | ✅ A 04/07 | `layout_bottom_nav.xml` TỰ CODE (bỏ BottomNavigationView) — icon căn giữa dọc/ngang mọi máy, vòng tròn tab đang chọn, chấm đỏ chuông realtime, hide-on-scroll. Include 1 dòng là dùng được |
| **NotificationFragment (tab chuông)** | ✅ A 04/07 | List notifications theo uid, chấm đỏ dòng UNREAD. TODO: bấm → đánh dấu đã đọc |
| **Sổ địa chỉ (LA.Address + DL_Add New Address)** | ✅ A 04/07 | `customers/{uid}/addresses`; người mới → tự mở form thêm, địa chỉ đầu = mặc định; Checkout TỰ chọn địa chỉ mặc định; dropdown 34 tỉnh/thành sau sáp nhập |
| **VoucherActivity (LA.Voucher1+2)** | ✅ A 04/07 | Tabs đếm số, kho trống 🎁, "Nhận mã từ Glassity" (BottomSheet claim → `customers/{uid}/vouchers`), mã khớp Checkout (FREESHIP/GIAM10/GIAM50K) |
| **ProfileFragment (LA.Profile)** | ✅ A 04/07 | Thẻ thành viên barcode + điểm, grid 5 tiện ích, list chính sách. Mã giảm giá → VoucherActivity; các mục khác Toast chờ nối |

### Fix quan trọng 04/07 (đọc kỹ)
1. **Auth**: mọi lần login Google đều upsert document `customers` (merge) — trước chỉ ghi khi `isNewUser`, ghi fail là mất data vĩnh viễn. Lỗi Firebase/Google hiện tiếng Việt kèm nguyên nhân (mã 7 = mạng, 10 = SHA-1, 12500 = Play Services cũ).
2. **Edge-to-edge (Android 15+)**: `utils/InsetsUtil.applySystemBarsPadding()` áp cho TẤT CẢ Activity — hết bị status bar/cụm camera che header, nút back bấm được trên mọi máy. **Activity mới PHẢI gọi 1 dòng này sau setContentView.**
3. **Emulator mất mạng**: boot kèm `-dns-server 8.8.8.8,1.1.1.1` (chạy từ terminal). Cả 2 AVD đều là bản Play Store, không cần tạo mới.
4. **`@DocumentId` (nhắc lại + case mới)**: KHÔNG ghi field id vào document. Case mới: field boolean `isDefault` → Firestore map theo getter thành `default` — ghi key `default` (xem AddressRepository).

---

## 🅱️ CHECKLIST HỒNG PHÚC (Person B — chỉ đụng `ui/catalog` + `adapter`)

⚠️ **Trước khi làm: merge `feature/person-MinhQuan` → `main` rồi pull** (04/07 có sửa Manifest, MainActivity, nav graph — làm trên code cũ sẽ conflict nặng).

- [ ] **[B-mới] Mô tả sản phẩm ~40 chữ**: `npm install firebase-admin && node update_descriptions.js` (cần service-account.json)
- [ ] **[B3] btnFilter trong ProductListActivity** — BottomSheet lọc giá/loại
- [ ] **[B4] HomeFragment** — 5 circle "chọn kính theo dạng mắt" → ProductListActivity có filter
- [ ] Style: nền trắng, nút đen `bg_btn_black_filled`, giá cam `color_price`
- [ ] Activity mới: thêm `<activity>` vào Manifest (không sửa cái có sẵn) + gọi `InsetsUtil.applySystemBarsPadding()`

---

## 🅰️ CHECKLIST MINH QUÂN (Person A)

- [ ] **[A4] OrderHistoryActivity + OrderDetailActivity** ⭐ ưu tiên 1
  - Tab trạng thái đơn; composite index `orders(customerId ASC, createdAt DESC)` (link trong Logcat lần chạy đầu)
  - Nối nút "XEM ĐƠN HÀNG" trong PaymentResult (đang Toast) + ô "Đơn hàng của bạn" trong Profile
- [ ] Bấm 1 thông báo → đánh dấu đã đọc (`status: READ`)
- [ ] **Test luồng A→Z bằng account đăng ký MỚI**: đăng ký → mua → checkout (tạo địa chỉ mới) → COD + Bank → check Firestore → xem đơn A4
- [ ] Front-end khớp Figma từng màn (chỉ màn của A) — so screenshot vs frame
- [ ] (Dư thời gian) SearchActivity đơn giản; Thông tin tài khoản (sửa tên/SĐT/đổi mật khẩu)

**Quy tắc git:** mỗi người 1 branch, xong task lớn → merge `main` → người kia pull. KHÔNG cùng sửa 1 file.

---

## ⚠️ LƯU Ý KHI CLONE / CHẠY

1. `service-account.json` KHÔNG có trên git (gitignore). Cần seed thì xin file từ Minh Quân, chạy `npm install firebase-admin && node seed_test_accounts.js`
2. Sau khi pull: **File → Reload All from Disk → Sync Gradle → Rebuild**
3. Đăng nhập test: `test1@glassity.com` / `Test@123`
4. Emulator lỗi mạng/Google Sign-In: xem mục "Fix quan trọng 04/07"
